/**
 * This is the entry point for your app
 * Include any assets to be bundled in here
 * (css/images/js/etc)
 */

require('regenerator-runtime');


if(!geotab.addin.async){
    
    require('./scripts/main');
    
}

require('./styles/main.css');
